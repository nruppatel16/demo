# --- Core runtime ---
alpaca-py>=0.43,<0.44           # Alpaca SDK: paper broker + market data (verified 0.43.4)
pydantic-settings>=2.0,<3       # typed config from .env
pandas>=2.0                     # bar/time-series
numpy>=1.24                     # metrics math
duckdb>=1.0                     # local cache + history + LLM cache
structlog>=24.0                 # structured logging

# --- Phase 3 ---
# (no extra deps — custom backtest engine uses pandas/numpy only)

# --- Phase 4 ---
APScheduler>=3.10,<4            # live-loop scheduler (v4 has different API; pin v3)

# --- Phase 5 ---
matplotlib>=3.7                 # equity-curve plots (saved as PNG, no display needed)

# --- Phase 6 (optional — LLM strategy) ---
# Remove if you don't want Claude as a strategy. Keep to benchmark it.
anthropic>=0.40                 # Anthropic Python SDK

# --- Dev / test ---
pytest>=8.0
